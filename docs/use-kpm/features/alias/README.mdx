import Since from "@site/src/components/Since"
import BelowDocument from "@site/src/components/BelowDocument"

# エイリアス

プラグインに別名を付けてかんたんにインストールします。

---

## 概要 {#overview}

エイリアスは、リモートによって管理されるプラグインに別名をつけられる機能です。これにより、 `ExamplePlugin` を、 `ex` のような短縮された別前でインストールできます。

<Since version={"v3"} />

---

## エイリアスを使う {#using-aliases}

エイリアスを用いたインストールは、通常のインストールと同じように、クエリを指定する場所にエイリアス名を指定します。

:::tip

これらは、[AliasResolver](../resolver#alias) によって解決されます。

:::

---

## エイリアスを作成する {#creating-aliases}

エイリアスを作成する方法は、別ページに分けられています。

<BelowDocument docId="use-kpm/features/alias/create" />
